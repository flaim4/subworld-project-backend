server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://172.18.248.2:3306/subworld
    username: flaim
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  messages:
    basename: messages
  web:
    locale: en

jwt:
  secret: mySuperSecretKeyThatIsAtLeast32CharactersLong123!
  expiration: 720

app:
  skins:
    directory: ./uploads/skins
    url-prefix: /skins
    default-skin: default.png
    max-size-mb: 10
