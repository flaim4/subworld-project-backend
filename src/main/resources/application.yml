server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://172.18.248.2:3306/subworld
    username: flaim
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  messages:
    basename: messages
  web:
    locale: en

jwt:
  secret: mySuperSecretKeyThatIsAtLeast32CharactersLong123!
  expiration: 720

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

app:
  skins:
    directory=skins:
logging:
  level:
    com.example: DEBUG
    org.springframework.security: DEBUG